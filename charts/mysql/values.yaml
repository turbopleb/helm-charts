# values.yaml for MySQL

image:
  repository: mysql
  tag: "8.0"
  pullPolicy: IfNotPresent

mysqlRootPassword: rootpassword123
mysqlUser: wpuser
mysqlPassword: wppassword123
mysqlDatabase: wordpress

service:
  type: ClusterIP
  port: 3306

persistence:
  enabled: true
  storageClass: "microk8s-hostpath"
  accessMode: ReadWriteOnce
  size: 10Gi

# No resource limits
resources: {}
